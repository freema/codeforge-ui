# codeforge-ui — ARCHIVED

> **This repository has been merged into the main [CodeForge monorepo](https://github.com/freema/codeforge).**
>
> The UI source code now lives in the `web/` directory of the main repo. This repository is no longer maintained.

## Migration

The UI was merged into `github.com/freema/codeforge` under `web/`. Development continues there as part of the monorepo.
